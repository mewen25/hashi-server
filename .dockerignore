.git
.gitignore
.venv
node_modules
.claude
.idea
.DS_Store

# huge raw sources — only the built .db files are needed at runtime
JMdict_e.xml
JMdict_e.gz

# regenerated inside the Dockerfile's dict-builder stage
jmdict.db
dict_fts.db

# writable runtime DBs live on the Railway volume and are seeded from R2 on
# first boot — never bake local copies into the image.
cards.db
cards.db-wal
cards.db-shm
requests.db
requests.db-wal
requests.db-shm
pronunciation.log.jsonl

# served from R2 — not needed in the container
audio
svg
jp_sounds

# no build script; served from R2 or not needed at runtime
mozc_dict.db

Dockerfile
.dockerignore
README.md
